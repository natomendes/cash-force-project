{
  "name": "cash-force-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint-staged:front": "cd frontend && npx lint-staged",
    "lint-staged:back": "cd backend && npx lint-staged",
    "build:back": "cd backend && npm run build",
    "build:front": "cd frontend && npm run build",
    "preup": "npm run build:back && npm run build:front",
    "up": "docker-compose up"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "git-commit-msg-linter": "^4.5.0",
    "husky": "^8.0.2",
    "lint-staged": "^13.1.0"
  }
}
